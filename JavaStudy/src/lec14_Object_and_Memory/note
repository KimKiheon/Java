자료형이 같아도 메모리의 생성이 다른곳에 되어있다면 엄연히 다른 객체이다

객체는 메모리에서 동적으로 생성되며, 이 객체체가 생성된 메모리의 주소 값만
레퍼런스가 가지고 있다.
ex)Class class = new Class();
여기서 class가 레퍼런스
레퍼런스: 생성한 객체의 주소를 변수에 저장한 것

객체가 필요 없게 되면 레퍼런스에서 연결을 끊어버리고 그 객체는
GC(garbage Collector)가 제거한다.

레퍼런스에 null이 저장되면 객체의 연결이 끊기며, 더 이상 객체를 이용할 수 없다.
