패키지: java 프로그램은 많은 클래스로 구성되고, 이러한 클래스를 
폴더 형식으로 관리하는 것

패키지 이름
-패키지 이름은 패키지에 속해 있는 클래스가 최대한 다른 클래스와
중복되는 것을 방지하도록 만든다.
-패키지 이름은 일반적으로 도메인을 거꾸로 이용
-개발 중 패키지의 이름과 구조는 변경 가능
-패키지 이름만 보고 해당 패키지 안에 있는 클래스가 어떤 속성과
기능을 가지고 있는지 예상이 될 수 있도록 만든다

import: 다른 패키지에 있는 클래스를 사용하기 위한 것

static: 클래스의 속성과 메서드에 static을 사용하면 어디서나 속성과
메서드를 공유할 수 있다 (like 전역변수) 다른 객체와 공유